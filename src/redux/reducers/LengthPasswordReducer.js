const INITIAL_STATE = {
  length: 0,
};

const lengthPassowordReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default lengthPassowordReducer;
