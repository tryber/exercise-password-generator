import React, { Component } from 'react';

export default class BtnGenerate extends Component {
  render() {
    return (
      <button type="submit">
        GENERATE PASSWORD
      </button>  
    );
  }
}
